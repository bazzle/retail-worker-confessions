var elem = document.querySelector('.flickity-carousel');
var flkty = new Flickity( elem, {
  // options
  pageDots: false,
  prevNextButtons: false,
  freeScroll: false,
  cellAlign: 'left',
  setGallerySize: true
});